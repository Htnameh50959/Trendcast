# Frontend (src)

React app source files.

Key folders
- `page/` — pages and UI screens
- `ui/` — shared UI components
- `utils/` — helper utilities

Run dev server from project root with `npm run dev`.
